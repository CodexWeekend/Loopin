{
  "name": "@loopin/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@loopin/geo": "workspace:*",
    "@loopin/shared": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src tests --ext .ts",
    "test:ci": "vitest run tests/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
