{
  "name": "@loopin/mobile",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@loopin/core": "workspace:*",
    "@loopin/shared": "workspace:*",
    "expo": "~53.0.20",
    "react": "^19.2.4",
    "react-native": "^0.79.6"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "expo start",
    "lint": "eslint App.tsx src tests --ext .ts,.tsx",
    "test:ci": "vitest run tests/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
