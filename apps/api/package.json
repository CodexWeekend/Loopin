{
  "name": "@loopin/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@loopin/core": "workspace:*",
    "@loopin/shared": "workspace:*",
    "fastify": "^5.6.1"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "node dist/index.js",
    "lint": "eslint src tests --ext .ts",
    "test:ci": "vitest run tests/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
